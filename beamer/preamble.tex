% Common Packages
% ===============

\PassOptionsToPackage{no-math}{fontspec}

\usepackage{kotex}
\usepackage{cancel}
\usepackage{ifxetex}
\ifxetex
  \usepackage{fontspec}
\fi
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{lipsum}

\usepackage[normalem]{ulem}
\usepackage{mathtools}
\usepackage{stackengine}
\usepackage{cancel}

% Beamer Settings
% ===============
\usetheme{metropolis}     % A default theme.
%\usetheme{Frankfurt}
%\usecolortheme{beaver}

% Use Compact Headline (no navigation bars or spaces)
\setbeamertemplate{headline}{}


% Font Settings
% =============
\ifxetex
%\setmainhangulfont{UnBatang}
%\setmainhanjafont {UnBatang}
%\setsanshangulfont{UnDotum}
\fi

% Use Roman-style fonts rather than Serif-style ones for math
\usefonttheme{professionalfonts}


% Styles
% ======

% Line Spread (120%)
\linespread{1.2}

% Use first-paragraph indents
%\usepackage{indentfirst}

% Paragraph lengths
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}

% Left margin of list/enumerate
\setlength{\leftmargini}{5pt}



% Beamer Customization
% ====================

\hypersetup{breaklinks=true,
            bookmarks=true,
            colorlinks=true,
            citecolor=blue,
            urlcolor=blue,
            linkcolor=magenta,
            pdfborder={0 0 0}}

% Get rid of navigation bars
\beamertemplatenavigationsymbolsempty

% Footline (Author, title, page number)
\setbeamertemplate{footline}
{
  \leavevmode%
  \hbox{%
  \begin{beamercolorbox}[wd=.2\paperwidth,ht=2.25ex,dp=1ex,left]{author in head/foot}%
    \hspace*{1ex}
    \usebeamerfont{author in head/foot}\insertshortauthor
  \end{beamercolorbox}%
  \begin{beamercolorbox}[wd=.8\paperwidth,ht=2.25ex,dp=1ex,right]{title in head/foot}%
    \usebeamerfont{title in head/foot}\insertshorttitle\hspace*{3em}
    \insertframenumber{} / \inserttotalframenumber\hspace*{1ex}
  \end{beamercolorbox}}%
  \vskip0pt%
}

% Bibliography item should show the reference number, e.g. [1]
\setbeamertemplate{bibliography item}{[\theenumiv]}
